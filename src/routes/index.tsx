// Route usage (landing): src/routes/index.tsx
// -------------------------------------------------------------
import { component$ } from '@builder.io/qwik';
import Navbar from '~/components/layout/Navbar';
export default component$(() => (
  <>
    <Navbar/>
  </>
));